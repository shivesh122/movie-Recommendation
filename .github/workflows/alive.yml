name: Keep Alive

on:
  schedule:
    - cron: "*/15 * * * *"   # every 5 minutes
  workflow_dispatch:        # lets you trigger manually if needed

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -s https://movie-recommendation-nbkm.onrender.com > /dev/null
